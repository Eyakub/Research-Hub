<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.easytechhub.helloeyakub.researchhub">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/appicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/appicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

            <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".account.ActivityLogin">
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!--
        <activity
            android:name=".activity.HomeActivity"
            android:label="Home" />
        <activity
            android:name=".activity.ProfileActivity"
            android:label="Profile" />
        <activity
            android:name=".activity.StudyActivity"
            android:label="Study" />
        -->
        <activity
            android:name=".ImageSlidingActivity"
            android:theme="@style/AppTheme" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".ScienceDetailsActivity" />
        <activity android:name=".BbaDetailsActivity" />
        <activity android:name=".account.ActivitySignup" />
        <activity android:name=".account.ActivityForgetPassword"></activity>
    </application>

</manifest>